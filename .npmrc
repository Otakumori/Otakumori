# NPM configuration for Otaku-mori v0
# Note: Removed deprecated options for future NPM compatibility

# Temporary deployment safety valve: allow Vercel/pnpm to refresh the lockfile
# when security-only dependency overrides are applied during install.
frozen-lockfile=false
