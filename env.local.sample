# === MISSING ENVIRONMENT VARIABLES FOR LOCAL DEVELOPMENT ===
# Based on Vercel audit results, add these to your .env.local file:

# CRITICAL: This variable is causing build failures
STRIPE_WEBHOOK_URL=https://www.otaku-mori.com/api/webhooks/stripe

# Missing client-side variables
NEXT_PUBLIC_CANONICAL_ORIGIN=https://www.otaku-mori.com
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_PROXY_URL=https://clerk.otaku-mori.com

# Missing feature flags
NEXT_PUBLIC_FEATURE_GA_ENABLED=false
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-YOUR_GA_ID_HERE
NEXT_PUBLIC_DAILY_PETAL_LIMIT=100

# Missing authentication keys (critical for Clerk to work)
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret_here
CLERK_ENCRYPTION_KEY=your_encryption_key_here

# Missing Printify configuration
PRINTIFY_API_URL=https://api.printify.com/v1/
PRINTIFY_SHOP_ID=your_shop_id_here
PRINTIFY_WEBHOOK_SECRET=your_printify_webhook_secret_here

# Missing Redis configuration
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# Missing email configuration
EMAIL_FROM=noreply@otaku-mori.com

# Missing security tokens
PETAL_SALT=your_petal_salt_here
CRON_SECRET=your_cron_secret_here
API_KEY=your_api_key_here

# Environment indicator
NODE_ENV=development

# === INSTRUCTIONS ===
# 1. Copy these variables to your .env.local file
# 2. Replace placeholder values with actual values from Vercel dashboard
# 3. The critical one is STRIPE_WEBHOOK_URL which is causing build failures
# 4. For development, you can use placeholder values for most services
# 5. Clerk keys are required for authentication to work
